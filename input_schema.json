{
    "title": "Trendyol Product Scraper (Ultra Fast & 90% Cheaper)",
    "description": "Scrapes products from Trendyol.com at lightning speed and 90% lower cost.",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "startUrls": {
            "title": "Start URLs",
            "type": "array",
            "description": "List of Trendyol URLs to scrape (can be category, search results, or specific product pages).",
            "editor": "requestListSources",
            "prefill": [
                { "url": "https://www.trendyol.com/sr?q=laptop" }
            ]
        },
        "maxItems": {
            "title": "Max Items",
            "type": "integer",
            "description": "Maximum number of items to scrape.",
            "default": 100,
            "minimum": 1
        },
        "proxyConfiguration": {
            "title": "Proxy Configuration",
            "type": "object",
            "description": "Choose your proxy settings. Datacenter proxies are recommended for cost efficiency.",
            "editor": "proxy",
            "prefill": {
                "useApifyProxy": true
            }
        }
    },
    "required": ["startUrls", "proxyConfiguration"]
}
